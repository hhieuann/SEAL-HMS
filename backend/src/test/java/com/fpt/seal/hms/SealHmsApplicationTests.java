package com.fpt.seal.hms;

import org.junit.jupiter.api.Test;

class SealHmsApplicationTests {

    @Test
    void contextLoadsPlaceholder() {
        // TODO: add @SpringBootTest once a test database (e.g. Testcontainers PostgreSQL) is configured.
        // Kept as a plain unit test so CI passes without a running DB.
    }
}
