const { Client } = require('pg'); const client = new Client({ user: 'postgres', host: 'localhost', database: 'seal_hms', password: '12345', port: 5432, }); client.connect(); client.query('TRUNCATE TABLE event CASCADE;', (err, res) => { if (err) { console.error('Error executing query', err.stack); } else { console.log('Database hackathon data cleaned successfully! Accounts are intact.'); } client.end(); });
